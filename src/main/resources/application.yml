spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:49164/clubtab
    username: postgres
    password: postgres
    initialization-mode: never
    continue-on-error: false
  jpa:
    show-sql: true
    generate-ddl: false